import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Item = () => {
    const item = useLoaderData()
    return (
        <div>
            
        </div>
    );
};

export default Item;